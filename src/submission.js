class Submission {
    constructor (options) {
        this.name = options.name; 
        this.bodyPart = options.bodyPart;
        this.subRate = options.subRate;
        this.fighter = options.fighter;
        this.videoLink = options.videoLink;

    }
}

module.exports = Submission;

